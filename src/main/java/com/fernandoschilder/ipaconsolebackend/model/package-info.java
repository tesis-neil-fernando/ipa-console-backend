/**
 * Este paquete contiene las entidades de la aplicación, que representan las tablas en la base de datos.
 * Cada clase en este paquete está mapeada a una tabla de base de datos mediante la anotación @Entity de JPA.
 * Las clases de este paquete incluyen tanto los modelos de datos como las relaciones entre las entidades.
 */

package com.fernandoschilder.ipaconsolebackend.model;